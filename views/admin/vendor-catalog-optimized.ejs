<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Optimized Vendor Catalog - Wavelength Admin</title>
    <link rel="icon" type="image/svg+xml" href="/icons/favicon.svg">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/admin-panel.css">
    
    <style>
        .optimized-catalog {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .performance-stats {
            background: #e8f5e8;
            border: 2px solid #28a745;
            border-radius: 6px;
            padding: 15px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .stat-item {
            text-align: center;
        }
        
        .stat-number {
            font-size: 24px;
            font-weight: bold;
            color: #28a745;
        }
        
        .stat-label {
            font-size: 12px;
            color: #666;
        }
        
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }
        
        .product-card {
            background: white;
            border: 2px solid #333;
            border-radius: 6px;
            padding: 15px;
            transition: transform 0.2s;
        }
        
        .product-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        
        .product-title {
            font-weight: bold;
            color: #007bff;
            margin-bottom: 8px;
        }
        
        .product-meta {
            font-size: 12px;
            color: #666;
            margin: 4px 0;
        }
        
        .image-placeholder {
            width: 100%;
            height: 120px;
            background: #f8f9fa;
            border: 1px dashed #ccc;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 10px 0;
            border-radius: 4px;
            cursor: pointer;
        }
        
        .image-loaded {
            background: none;
            border: 1px solid #ddd;
        }
        
        .image-loaded img {
            width: 100%;
            height: 120px;
            object-fit: cover;
            border-radius: 4px;
        }
        
        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid #f3f3f3;
            border-top: 2px solid #007bff;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .load-more {
            text-align: center;
            margin: 20px 0;
        }
        
        .load-more button {
            padding: 12px 24px;
            background: #28a745;
            color: white;
            border: 2px solid #28a745;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
        }
        
        .load-more button:hover {
            background: #218838;
        }
        
        .load-more button:disabled {
            background: #6c757d;
            border-color: #6c757d;
            cursor: not-allowed;
        }
    </style>
</head>

<body>
    <%- include('../partials/header') %>
    
    <div class="main-wrapper">
        <div class="optimized-catalog">
            <div class="catalog-header">
                <h1>⚡ Optimized Vendor Catalog</h1>
                <p>Fast-loading catalog with lazy image loading and pagination</p>
            </div>
            
            <div class="performance-stats">
                <div class="stat-item">
                    <div class="stat-number" id="load-time">--</div>
                    <div class="stat-label">Load Time (ms)</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" id="product-count">0</div>
                    <div class="stat-label">Products Loaded</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" id="images-loaded">0</div>
                    <div class="stat-label">Images Loaded</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" id="api-calls">0</div>
                    <div class="stat-label">API Calls</div>
                </div>
            </div>
            
            <div id="products-container">
                <div class="products-grid" id="products-grid">
                    <!-- Products will be loaded here -->
                </div>
                
                <div class="load-more">
                    <button id="load-more-btn" onclick="loadMoreProducts()">
                        Load More Products
                    </button>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Performance tracking
        const startTime = Date.now();
        let currentPage = 1;
        let totalPages = 1;
        let isLoading = false;
        let apiCallCount = 0;
        let imagesLoadedCount = 0;
        
        // Load initial products
        document.addEventListener('DOMContentLoaded', function() {
            loadProducts(1);
        });
        
        async function loadProducts(page = 1) {
            if (isLoading) return;
            
            isLoading = true;
            apiCallCount++;
            updateStats();
            
            try {
                const response = await fetch(`/admin/vendor-catalog/api?page=${page}&limit=20`);
                const data = await response.json();
                
                if (data.success) {
                    if (page === 1) {
                        // First load - replace content
                        document.getElementById('products-grid').innerHTML = '';
                    }
                    
                    // Add products to grid
                    data.products.forEach(product => {
                        addProductToGrid(product);
                    });
                    
                    // Update performance stats
                    document.getElementById('product-count').textContent = 
                        document.querySelectorAll('.product-card').length;
                    
                    if (page === 1) {
                        const loadTime = Date.now() - startTime;
                        document.getElementById('load-time').textContent = loadTime;
                    }
                    
                    // Hide load more if no more products
                    if (data.products.length < 20) {
                        document.getElementById('load-more-btn').style.display = 'none';
                    }
                }
            } catch (error) {
                console.error('Error loading products:', error);
            } finally {
                isLoading = false;
            }
        }
        
        function addProductToGrid(product) {
            const grid = document.getElementById('products-grid');
            
            const productCard = document.createElement('div');
            productCard.className = 'product-card';
            productCard.innerHTML = `
                <div class="product-title">${product.title || 'Product'}</div>
                <div class="product-meta">
                    <strong>Product ID:</strong> ${product.productId || product.id}
                </div>
                <div class="product-meta">
                    <strong>Product Type:</strong> ${product.blueprintName || 'Unknown'}
                </div>
                <div class="product-meta">
                    <strong>Print Provider:</strong> ${product.providerName || 'Unknown'}
                </div>
                <div class="product-meta">
                    <strong>Source:</strong> ${product.sourceImage || 'N/A'}
                </div>
                <div class="product-meta">
                    <strong>Created:</strong> ${product.createdAt ? new Date(product.createdAt).toLocaleDateString() : 'Unknown'}
                </div>
                
                <div class="image-placeholder" onclick="loadProductImage('${product.productId || product.id}', this)">
                    <span>Click to Load Image</span>
                </div>
                
                <div class="product-actions" style="margin-top: 15px; text-align: center;">
                    <a href="/merchandise/preview/${product.productId || product.id}" 
                       class="btn" target="_blank" style="display: inline-block; padding: 6px 12px; background: #007bff; color: white; text-decoration: none; border-radius: 4px; font-size: 12px;">
                        👁️ View
                    </a>
                </div>
            `;
            
            grid.appendChild(productCard);
        }
        
        async function loadProductImage(productId, placeholder) {
            if (placeholder.classList.contains('image-loaded')) return;
            
            placeholder.innerHTML = '<div class="loading-spinner"></div>';
            apiCallCount++;
            updateStats();
            
            try {
                // Simulate image loading for demo
                setTimeout(() => {
                    placeholder.innerHTML = '<span style="color: #999;">Demo Image Placeholder</span>';
                    placeholder.classList.add('image-loaded');
                    imagesLoadedCount++;
                    updateStats();
                }, 500);
            } catch (error) {
                console.error('Error loading image:', error);
                placeholder.innerHTML = '<span style="color: #dc3545;">Failed to Load</span>';
            }
        }
        
        function loadMoreProducts() {
            currentPage++;
            loadProducts(currentPage);
        }
        
        function updateStats() {
            document.getElementById('api-calls').textContent = apiCallCount;
            document.getElementById('images-loaded').textContent = imagesLoadedCount;
        }
    </script>
</body>
</html>